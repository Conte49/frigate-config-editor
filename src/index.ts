/**
 * Frigate Config Editor — entry point.
 *
 * This module is bundled as a single ES module and served to Home Assistant
 * as a custom panel. HA will load it and then instantiate the registered
 * custom element (<frigate-config-editor>) inside an iframe-less panel.
 */
import './frigate-config-editor.js';
